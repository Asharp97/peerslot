meta {
  name: Publish booking page
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/booking-page
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  content-type: application/json
  origin: {{baseUrl}}
}

body:json {
  {
    "isPublished": true
  }
}

tests {
  test("provider can publish the page", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().bookingPage.isPublished).to.equal(true);
  });
}
