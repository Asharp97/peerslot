meta {
  name: Get Google sign-in URL
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/api/auth/sign-in/social
  body: json
  auth: none
}

headers {
  content-type: application/json
  origin: {{baseUrl}}
}

body:json {
  {
    "provider": "google",
    "callbackURL": "{{baseUrl}}",
    "disableRedirect": true
  }
}

tests {
  test("Google OAuth is configured or explicitly unavailable", function () {
    expect(res.getStatus()).to.be.oneOf([200, 400, 404]);
    if (res.getStatus() === 200) {
      expect(res.getBody().url).to.include("accounts.google.com");
    }
  });
}
